name: app_tutorial
description: Package for creating onboarding flows for your app to explain the features of the app in a seamless ways to you users.
version: 1.0.6
homepage: https://github.com/aikenahac/app-tutorial

environment:
  sdk: '>=2.19.3 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
